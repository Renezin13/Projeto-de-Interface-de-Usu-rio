function Cookie({ onClick }) {
    return (
        <button 
            onClick={onClick} 
            style={{ fontSize: "40px", padding: "20px", cursor: "pointer" }}
        >
            🍪
        </button>
    )
}

export default Cookie
